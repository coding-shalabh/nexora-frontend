/**
 * Sales Deals Page
 *
 * Re-exports the DealsPage component from pipeline.
 * The Next.js config redirects /pipeline/deals → /sales/deals,
 * so this page renders the actual deals content.
 */
export { default } from '@/app/(dashboard)/pipeline/deals/page';
