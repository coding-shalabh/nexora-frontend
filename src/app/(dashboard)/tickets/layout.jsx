'use client';

/**
 * Tickets Layout
 *
 * Minimal wrapper - each page uses UnifiedLayout with its own config.
 * This allows per-page customization of stats, actions, and fixedMenu.
 */
export default function TicketsLayout({ children }) {
  return <>{children}</>;
}
